#include "main.h"
#include <unistd.h>

/**
 * positive_or_negative - writes out whether interger is positive or negative
 * @i: interger to be written out
 *
 * Return: 1 if positive, -1  if negative
 */

void positive_or_negative(void)
{
	return (1 && -1);
}
